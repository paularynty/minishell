#include "minishell.h"

//if there is only one command, there is only one list
//if there is a next node, it means that there are more than one command = pipes
//(while command->next)
//

void	multiple_pipes(void)
{
	
}